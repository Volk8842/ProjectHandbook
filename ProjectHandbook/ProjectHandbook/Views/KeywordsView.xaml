<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ProjectHandbook.Views.KeywordsView">
    <ContentView.Content>
        <Grid Margin="0" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Frame
                Grid.Row="0"
                Grid.RowSpan="2"
                Padding="0"
                BackgroundColor="#E8E8E7"
                CornerRadius="0"
                HasShadow="False" />
            <Label
                Grid.Row="0"
                Margin="5,5"
                FontAttributes="Bold"
                FontSize="Small"
                HorizontalTextAlignment="Start"
                Text="KEYWORDS:"
                TextColor="Black"
                VerticalTextAlignment="Center" />
            <Label
                Grid.Row="1"
                Margin="15,5,5,5"
                FontAttributes="Bold"
                FontSize="Small"
                HorizontalTextAlignment="Start"
                Text="Tyranids, Infantry, Commander, Much, More, Keywords"
                TextColor="Black"
                LineBreakMode="WordWrap"
                VerticalTextAlignment="Center" />
        </Grid>
    </ContentView.Content>
</ContentView>